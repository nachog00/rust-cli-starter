[package]
name = "{{project-name}}-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Application services (implements driving ports over driven ports)"

[dependencies]
{{project-name}}-core = { workspace = true }

[dev-dependencies]
{{project-name}}-core = { workspace = true, features = ["test-support"] }
